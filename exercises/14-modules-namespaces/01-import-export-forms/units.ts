/**
 * Constants only. Imported as a namespace, this becomes a lookup table whose
 * keys are known to the type system.
 */

export const mm = 0.001;
export const cm = 0.01;
export const m = 1;
export const km = 1000;
